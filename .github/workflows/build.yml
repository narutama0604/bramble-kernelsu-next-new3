name: Build KernelSU Next for Pixel 4a 5G
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 空き容量の確保
        run: |
          sudo rm -rf /usr/share/dotnet /etc/mysql /var/lib/mysql /usr/local/lib/android
          sudo apt update && sudo apt install -y bc build-essential flex bison libssl-dev libelf-dev clang lld llvm git

      - name: ソースとツールチェーンの取得
        run: |
          # 1. カーネルソース (LineageOS 21 / Android 14)
          git clone --depth=1 https://github.com/LineageOS/android_kernel_google_redbull -b lineage-21 kernel-source
          
          # 2. KernelSU Next
          cd kernel-source
          git clone https://github.com/rifsxd/KernelSU-Next drivers/kernelsu-next
          
          # 3. ツールチェーン (LineageOS用 Clang)
          cd ..
          git clone --depth=1 https://gitlab.com/LineageOS/binaries/android_prebuilts_clang_kernel_linux-x86_clang-r487747c clang

      - name: カーネルへのパッチ適用
        run: |
          cd kernel-source
          
          # drivers/Kconfig の最後にある endmenu の直前に追記
          sed -i '/endmenu/i source "drivers/kernelsu-next/Kconfig"' drivers/Kconfig
          
          # drivers/Makefile に追記
          echo 'obj-y += kernelsu-next/' >> drivers/Makefile
          
          # コンフィグの特定と KSU 有効化
          # LineageOS 21 の場合、通常は arch/arm64/configs/vendor/redbull_defconfig
          CONF="arch/arm64/configs/vendor/redbull_defconfig"
          if [ -f "$CONF" ]; then
            echo "CONFIG_KSU=y" >> "$CONF"
            echo "CONFIG_KSU_NEXT_RENAME_UNTRUSTED_APP=y" >> "$CONF"
          else
            echo "Error: $CONF not found. Searching for alternatives..."
            find arch/arm64/configs/ -name "*defconfig" -exec sh -c 'echo "CONFIG_KSU=y" >> "$1"' -- {} \;
          fi

      - name: ビルド実行
        run: |
          cd kernel-source
          export PATH=$GITHUB_WORKSPACE/clang/bin:$PATH
          
          # ビルドフラグの設定
          MAKE_ARGS="ARCH=arm64 \
                     O=out \
                     CC=clang \
                     LD=ld.lld \
                     AR=llvm-ar \
                     NM=llvm-nm \
                     OBJCOPY=llvm-objcopy \
                     OBJDUMP=llvm-objdump \
                     STRIP=llvm-strip \
                     CROSS_COMPILE=aarch64-linux-gnu- \
                     CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
                     CLANG_TRIPLE=aarch64-linux-gnu-"

          # 構成の生成
          make $MAKE_ARGS vendor/redbull_defconfig
          
          # コンパイル (GitHub Actionsのコア数をフル活用)
          make -j$(nproc --all) $MAKE_ARGS

      - name: 成果物のパッケージング (AnyKernel3)
        run: |
          # Image.lz4-dtb が生成されているか確認して AnyKernel3 で固める
          git clone https://github.com/osm0sis/AnyKernel3
          rm -rf AnyKernel3/.git
          cp kernel-source/out/arch/arm64/boot/Image.lz4-dtb AnyKernel3/
          cd AnyKernel3
          # Pixel 4a 5G 用に AnyKernel3 の設定を調整
          sed -i 's/device.name1=maguro/device.name1=bramble/g' anykernel.sh
          sed -i 's/device.name2=toro/device.name2=redfin/g' anykernel.sh
          zip -r ../ksu-next-kernel.zip *

      - name: 成果物のアップロード
        uses: actions/upload-artifact@v4
        with:
          name: ksu-next-kernel-zip
          path: ksu-next-kernel.zip
